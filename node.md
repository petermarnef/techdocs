# Test an NPM file locally

- Create a tar package with `npm pack` (make sure the `dist` folder is created / the project is build)
- Do an `npm install <tar file>` in the folder of the target project
